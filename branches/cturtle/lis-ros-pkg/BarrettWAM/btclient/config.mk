# config.mk -- Configuration details

# Define the target operating system
# TARGET_OS = rtai
# TARGET_OS = xenomai

TARGET_OS=xenomai

# Define the CANbus hardware installed
# CAN_HARDWARE = esd
# CAN_HARDWARE = peak

CAN_HARDWARE=peak

# Define the type of CAN interface
# CAN_TYPE = isa
# CAN_TYPE = pci

CAN_TYPE=pci

